<nav>
    <div class="nav-element">
        <a href="{{ path('utilisateur_liste') }}"><h3>Accueil</h3></a>
        <ul class="navbar-nav">
            {% if app.user %}
                <li class="nav-item">
                    <a class="" aria-current="page" href="{{ path('app_logout') }}"><h5>Se Deconnecter ({{ app.user.username }})</h5></a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="bouton-nav bouton-connection" aria-current="page" href="{{ path('app_login') }}">Se connecter</a>
                </li>
                <li class="nav-item">
                    <a class="bouton-nav bouton-inscription" aria-current="page" href="{{ path('app_register') }}">S'inscrire</a>
                </li>
            {% endif %}
        </ul>
    </div>

</nav>